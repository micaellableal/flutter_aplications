import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:primeiro_app/app/modules/login/pages/register/register_page_page.dart';

void main() {
  testWidgets('RegisterPagePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegisterPagePage(title: 'RegisterPage')));
    //  final titleFinder = find.text('RegisterPage');
    //  expect(titleFinder, findsOneWidget);
  });
}
